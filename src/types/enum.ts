export enum Sort {
  Created = 'created',
  Updated = 'updated',
  Comments = 'comments',
}

export enum State {
  All = 'all',
  Open = 'open',
  Closed = 'closed',
}

export enum Direction {
  Asc = 'asc',
  Desc = 'desc',
}

export enum RepositorySort {
  Created = 'created',
  Updated = 'updated',
  Pushed = 'pushed',
  FullName = 'full_name',
}

export enum RepositoryType {
  All = 'all',
  Owner = 'owner',
  Member = 'member',
}
